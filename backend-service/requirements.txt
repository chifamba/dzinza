fastapi[all]>=0.100.0
pydantic-settings>=2.0.0
httpx>=0.25.0 # For making async HTTP requests to downstream services

# For JWT validation if the gateway handles it
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4 # Usually for hashing, but often included with JWT setups

# For rate limiting (example, choose one or implement custom)
# slowapi # (Starlette/FastAPI compatible) - check for latest version

# For OpenTelemetry (optional)
opentelemetry-api>=1.19.0
opentelemetry-sdk>=1.19.0
opentelemetry-instrumentation-fastapi>=0.40b0
opentelemetry-instrumentation-httpx>=0.40b0 # To trace outgoing requests
opentelemetry-exporter-otlp-proto-http>=1.19.0

# For Prometheus metrics (optional)
starlette-prometheus>=0.9.0

# Development/Testing
pytest>=7.0.0
pytest-asyncio>=0.20.0
# respx # For mocking httpx requests in tests (alternative to pytest-httpx)
