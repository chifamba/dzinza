apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: mediastorageservice
spec:
  hosts:
    - "mediastorageservice.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: mediastorageservice
            port:
              number: 8080
