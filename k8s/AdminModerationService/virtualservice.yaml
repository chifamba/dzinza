apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: adminmoderationservice
spec:
  hosts:
    - "adminmoderationservice.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: adminmoderationservice
            port:
              number: 8080
