apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: searchdiscoveryservice
spec:
  hosts:
    - "searchdiscoveryservice.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: searchdiscoveryservice
            port:
              number: 8080
