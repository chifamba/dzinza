apiVersion: v1
kind: Service
metadata:
  name: audithistoryservice
  labels:
    app: audithistoryservice
spec:
  type: ClusterIP
  selector:
    app: audithistoryservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
