apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: audithistoryservice
spec:
  hosts:
    - "audithistoryservice.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: audithistoryservice
            port:
              number: 8080
