apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: graphqueryservice
spec:
  hosts:
    - "graphqueryservice.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: graphqueryservice
            port:
              number: 8080
