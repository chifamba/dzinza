apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: extendedservicesopenapi
spec:
  hosts:
    - "extendedservicesopenapi.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: extendedservicesopenapi
            port:
              number: 8080
