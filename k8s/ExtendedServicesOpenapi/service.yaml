apiVersion: v1
kind: Service
metadata:
  name: extendedservicesopenapi
  labels:
    app: extendedservicesopenapi
spec:
  type: ClusterIP
  selector:
    app: extendedservicesopenapi
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
