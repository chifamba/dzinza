apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: genealogyservice
spec:
  hosts:
    - "genealogyservice.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: genealogyservice
            port:
              number: 8080
