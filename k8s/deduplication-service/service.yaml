apiVersion: v1
kind: Service
metadata:
  name: deduplication-service
  labels:
    app: deduplication-service
spec:
  type: ClusterIP
  selector:
    app: deduplication-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
