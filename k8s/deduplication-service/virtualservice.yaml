apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: deduplication-service
spec:
  hosts:
    - "deduplication-service.local"
  gateways:
    - shared-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: deduplication-service
            port:
              number: 8080
