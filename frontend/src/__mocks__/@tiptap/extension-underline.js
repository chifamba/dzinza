// src/__mocks__/@tiptap/extension-underline.js
const Underline = {
  // Assuming Underline is an object/extension added directly
  // and does not need .configure()
  name: 'underline-mock',
};
export default Underline;
