{
  "name": "dzinza-storage-service",
  "version": "1.0.0",
  "description": "Dzinza Storage Service - File and Media Management",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --watchAll=false", 
    "lint": "eslint . --fix",
    "lint:check": "eslint .",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}
